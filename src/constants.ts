/** The EnConvert API base URL. Every request goes here. */
export const API_BASE = 'https://api.enconvert.com';

/** The header EnConvert authenticates with. Not `Authorization: Bearer`. */
export const API_KEY_HEADER = 'X-API-Key';
